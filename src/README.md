# cpp-utils
Code written in C++, in development stage, to be copied to relevant projects as needed

**Essential classes:**

- `DAGsampler`  MCMC interface. Call various move sequences in the DAG space.
- `DDAG`  Dynamic DAG. Add and remove arcs, ask ancestor queries.
- `(TBA)`

**Test programs:**

- `test`  Currently just an example usage of DAGsampler.
